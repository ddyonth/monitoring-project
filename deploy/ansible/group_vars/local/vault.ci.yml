---
# Placeholder для CI-деплоя (джоба deploy-server в .github/workflows/ci.yml).
# Настоящий vault.yml в репозиторий не попадает; playbook.yml подключает
# первый существующий из [vault.yml, vault.ci.yml], а реальные значения
# в CI приходят через --extra-vars (у них приоритет выше vars_files).
# Файл должен оставаться валидным пустым словарём, не пустым файлом.
{}
