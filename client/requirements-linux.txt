# Зависимости клиентского агента для сборки/запуска под Linux (Astra/ALT).
# Версии — те же, что в корневом requirements.txt; без Windows-only пакетов
# (pywin32-ctypes, pefile нужны PyInstaller только под Windows) и без
# серверных зависимостей. Используется job build-linux-agent в CI.
psutil==7.1.3
requests==2.32.5
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11
urllib3==2.5.0

# сборка бинарника
pyinstaller==6.17.0
pyinstaller-hooks-contrib==2025.10
altgraph==0.17.5
packaging==25.0
setuptools==80.9.0
